<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Shop.Presentation.FooterView">
    
    <Border Background="{StaticResource BackgroundColor}" Padding="20" Margin="0 10 0 0">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" OffsetY="-2" Opacity="0.5"/>
        </Border.Effect>
        <Grid ColumnDefinitions="*, Auto, *">
            <TextBlock 
                FontWeight="Bold"
                FontSize="14"
                Margin="10"
                Grid.Column="1"
                HorizontalAlignment="Center">© 2024 Arne's Shop - Alle rettigheder forbeholdes.</TextBlock>
        </Grid>
    </Border>
</UserControl>